import { type ReactiveController } from 'lit';
import { type OmniAssistChatCircle } from './omni-assist-chat-circle.js';
import { type OmniAssistWithPlugins } from './omni-assist-with-plugins.js';
type Corner = 'TL' | 'BL' | 'TR' | 'BR';
export declare class OmniAssistDragController implements ReactiveController {
    #private;
    private host;
    cornerGravity: boolean;
    offsetLeft: number;
    corner: Corner;
    hEdgeOffset: number;
    vEdgeOffset: number;
    constructor(host: OmniAssistChatCircle | OmniAssistWithPlugins, cornerGravity?: boolean, offsetLeft?: number);
    hostConnected(): void;
    setPosition: (e?: any, animate?: boolean) => void;
    get tooltipPlace(): "left" | "right";
    hostDisconnected(): void;
}
export {};
//# sourceMappingURL=omni-assist-drag-controller.d.ts.map