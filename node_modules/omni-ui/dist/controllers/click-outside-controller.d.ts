import type { ReactiveController, ReactiveElement } from 'lit';
import type { OmniElement } from '../omni-element.js';
export interface OpenableElement extends ReactiveElement {
    opened: boolean;
}
/**
 * - if your element has an 'opened' prop, it will automatically set to 'false' on outside click
 * - else, provide a function to call for closing
 */
export declare class ClickOutsideController implements ReactiveController {
    #private;
    private host;
    private closeFn;
    constructor(host: OpenableElement | OmniElement, closeFn?: (e: PointerEvent) => void);
    hostConnected(): void;
    hostDisconnected(): void;
}
//# sourceMappingURL=click-outside-controller.d.ts.map